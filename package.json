{
  "name": "pi-memory",
  "version": "1.0.0",
  "description": "Pi agent extension: structured long-term memory — store, recall, forget via the host journal.",
  "keywords": [
    "pi-package",
    "memory",
    "pi-extension"
  ],
  "license": "MIT",
  "repository": "mohammadraufzahed/pi-memory",
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typebox": "^1.3.34",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
